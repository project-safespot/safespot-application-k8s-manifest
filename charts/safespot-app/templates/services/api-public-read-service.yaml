apiVersion: v1
kind: Service
metadata:
  name: api-public-read
  namespace: {{ .Values.namespace }}
  labels:
    app: api-public-read
    env: {{ .Values.env | default "dev" }}
spec:
  selector:
    app: api-public-read
  ports:
    - port: 8080
      targetPort: 8080
  type: ClusterIP
