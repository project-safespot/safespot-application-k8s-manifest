{{- if .Values.apiPublicRead.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Values.apiPublicRead.name }}
  namespace: {{ .Values.global.namespace }}
  labels:
    app: {{ .Values.apiPublicRead.name }}
    service: {{ .Values.apiPublicRead.name }}
    env: {{ .Values.global.env }}
spec:
  {{- if not .Values.autoscaling.apiPublicRead.enabled }}
  replicas: {{ .Values.apiPublicRead.replicas }}
  {{- end }}
  selector:
    matchLabels:
      app: {{ .Values.apiPublicRead.name }}
  template:
    metadata:
      labels:
        app: {{ .Values.apiPublicRead.name }}
        service: {{ .Values.apiPublicRead.name }}
        env: {{ .Values.global.env }}
    spec:
      serviceAccountName: {{ .Values.apiPublicRead.name }}
      containers:
        - name: {{ .Values.apiPublicRead.name }}
          image: "{{ .Values.apiPublicRead.image.repository }}:{{ .Values.apiPublicRead.image.tag }}"
          imagePullPolicy: {{ .Values.apiPublicRead.image.pullPolicy }}
          ports:
            - name: http
              containerPort: {{ .Values.apiPublicRead.port }}
          envFrom:
            - configMapRef:
                name: safespot-api-public-read-config
            - secretRef:
                name: {{ .Values.secret.existingName }}
          resources:
            {{- toYaml .Values.apiPublicRead.resources | nindent 12 }}
          startupProbe:
            httpGet:
              path: {{ .Values.apiPublicRead.probes.startupPath }}
              port: http
            timeoutSeconds: 5
            periodSeconds: 10
            failureThreshold: 30
          readinessProbe:
            httpGet:
              path: {{ .Values.apiPublicRead.probes.readinessPath }}
              port: http
            timeoutSeconds: 5
            periodSeconds: 5
            failureThreshold: 6
          livenessProbe:
            httpGet:
              path: {{ .Values.apiPublicRead.probes.livenessPath }}
              port: http
            timeoutSeconds: 5
            periodSeconds: 10
            failureThreshold: 3
{{- end }}
