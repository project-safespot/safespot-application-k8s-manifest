apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: api-public-read-netpol
  namespace: {{ .Values.namespace }}
  labels:
    app: api-public-read
    env: {{ .Values.env | default "dev" }}
spec:
  podSelector:
    matchLabels:
      app: api-public-read
  policyTypes:
    - Ingress
    - Egress
  ingress:
    - from:
        - podSelector:
            matchLabels:
              app: ingress
      ports:
        - port: 8080
  egress:
    - {}
